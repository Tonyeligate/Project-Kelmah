   # Server Settings
   NODE_ENV=development
   PORT=5000
   FRONTEND_URL=http://localhost:3000

   # JWT Configuration
   JWT_SECRET=deladem
   JWT_REFRESH_SECRET=deladem
   JWT_EXPIRES=1h
   JWT_REFRESH_EXPIRES=7d

   # MongoDB Connection (Mongoose)
   DB_HOST=localhost
   DB_PORT=27017
   DB_NAME=kelmah
   # Uncomment & set if you require auth on Mongo
   # DB_USER=
   # DB_PASSWORD=

   # PostgreSQL Connection (Sequelize)
   SQL_DIALECT=postgres
   SQL_DB_HOST=localhost
   SQL_DB_PORT=5433
   SQL_DB_NAME=kelmah
   SQL_DB_USER=postgres
   SQL_DB_PASSWORD=deladem

   # Email Settings
   EMAIL_FROM=no-reply@kelmah.com

   # Rate Limiting
   RATE_LIMIT_WINDOW_MS=900000
   RATE_LIMIT_MAX=100