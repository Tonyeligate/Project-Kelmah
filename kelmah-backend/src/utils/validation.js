/**
 * Validation utility that simply re-exports the validate middleware.
 * This keeps backward compatibility for modules importing ../utils/validation.
 */

module.exports = require('../middlewares/validation'); 