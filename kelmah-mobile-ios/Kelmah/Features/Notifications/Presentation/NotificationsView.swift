import SwiftUI

struct NotificationsView: View {
    var body: some View {
        PlaceholderStateView(
            title: "Notifications",
            subtitle: "Prepared for professional push routing, alert prioritization, and background delivery handling.",
            bullets: [
                "Device registration support via future backend device-token endpoint.",
                "Operational alerting, job activity, and message notifications.",
                "Safe UX for denied permissions and delayed delivery conditions."
            ]
        )
    }
}
