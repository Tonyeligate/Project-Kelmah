# Render-based backend connection for production deployment
# Backend runs on Render, frontend connects via Render URL
# API_URL includes /api prefix since all gateway routes are under /api/*
VITE_API_URL=https://kelmah-api-gateway-hkke.onrender.com/api
VITE_WS_URL=wss://kelmah-api-gateway-hkke.onrender.com
# Set this locally if needed for development, e.g.:
# VITE_API_URL=http://localhost:5000
