# VITE_API_URL intentionally unset for production builds on Vercel.
# The app defaults to using the API Gateway via relative '/api' to avoid mixed-content.
# Set this locally if needed, e.g.:
# VITE_API_URL=http://localhost:5000
