import React from 'react';
import { Box, Typography } from '@mui/material';

const SystemSettings = () => {
  return (
    <Box>
      <Typography variant="h6">SystemSettings</Typography>
    </Box>
  );
};

export default SystemSettings;
